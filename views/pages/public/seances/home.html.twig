{% extends 'pages/public/list.html.twig' %}

{% block entity_title %}Les séances de la semaine :{% endblock %}

{% block entity_add_link %}{% endblock %}

{% block entity_table_headers %}
    <th>Titre</th>
    <th>Horaire</th>
    <th>Salle</th>
    <th>Doublage</th>
{% endblock %}

{% block entity_table_content %}
    <td><a href="{{ app.url_generator.generate('public-films-detail', {id: entity.film.id}) }}">{{ entity.film.titre }}</a></td>
    <td>{{ entity.dateSeance | date("d/m/Y H:i") }}</td>
    <td>{{ entity.salle.nom }}</td>
    <td>{{ entity.doublage }}</td>
{% endblock %}
