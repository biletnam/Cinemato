{% extends 'pages/intranet/detail.html.twig' %}

{% block entity_title %}Film{% endblock %}

{% block entity_table %}
    <tr>
        <th>Id</th>
        <td>{{ entity.id }}</td>
    </tr>
    <tr>
        <th>Titre</th>
        <td>{{ entity.titre }}</td>
    </tr>
    <tr>
        <th>Date de sortie</th>
        <td>{{ entity.dateDeSortie|date('d-m-Y') }}</td>
    </tr>
    <tr>
        <th>Âge minimum requis</th>
        <td>{{ entity.ageMinimum }}</td>
    </tr>
    <tr>
        <th>Genre</th>
        <td>{{ entity.genre }}</td>
    </tr>
    <tr>
        <th>Distributeur</th>
        <td>{{ entity.distributeur }}</td>
    </tr>
{% endblock %}

{% block entity_list_path %}href='{{ app.url_generator.generate('intranet-films-list') }}'{% endblock %}

{% block entity_edit_path %}href='{{ app.url_generator.generate('intranet-films-edit', { 'id': entity.id }) }}'{% endblock %}

{% block entity_states_link %}{% endblock %}

{% block entity_delete_path %}action='{{ app.url_generator.generate('intranet-films-delete', { 'id': entity.id }) }}'{% endblock %}
